export interface iHeader {
    startBtn?: Boolean,
    loginBtn?: Boolean,
    registerBtn?: Boolean,
    profileBtn?: Boolean,
    logoutBtn?: Boolean,
    adminLogoutBtn?: Boolean,
    searchBtn?: Boolean,
    adminPanel?: Boolean
}